# 直播选人台 · 服务端环境变量示例
# 复制为 .env 后填入真实值。.env 已被 .gitignore 忽略, 不会提交。
# 三个数据源相互独立: 任一未配置只会让对应数据缺失, 不影响其它源与选人流程。

# Steam 官方 Web API Key —— 头像 / 昵称 / 游戏时长, 以及自定义 URL(vanity)解析。
# 申请: https://steamcommunity.com/dev/apikey
# 未配置时: 只能用 SteamID64 / STEAM_0 / profiles 链接绑定, 且无头像与时长。
STEAM_API_KEY=

# Faceit Data API Key —— CS2 段位 / ELO / K-D / 胜率。
# 申请: https://developers.faceit.com/ (创建应用后取 Server Side API Key)
# 未配置时: CS2 数据整体不可用(玩家卡片上无 CS2 段位)。
FACEIT_API_KEY=

# WebSocket / HTTP 服务端口 (默认 8787)。
# DRAFT_WS_PORT=8787

# 说明: OpenDota (Dota2 段位/胜率/常用英雄) 免费且无需 key, 始终可用。
